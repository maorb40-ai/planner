# חילוץ חירום — נעילה, נתונים שנעלמו, וגרסה ישנה שנתקעה

שמור את הדף הזה. הוא פותר את שלושת המצבים שאפשר להיתקע בהם.

---

## 1. הדשבורד נעול ואני לא מצליח להיכנס

### הדרך הקלה (מגרסה 3.5.1 והלאה)

במסך הנעילה יש כפתור **"שכחתי את הסיסמה"**. לחיצה עליו פותחת:

* **שחזור וביטול הנעילה** — בוחרים גרסה שמורה ולוחצים. הנתונים חוזרים והנעילה מתבטלת.
* **הורדת גיבוי לקובץ** — לפני שמשחזרים, כדאי להוריד עותק.
* **טעינת קובץ גיבוי JSON** — אם יש לך גיבוי שייצאת בעבר.

### אם אתה בגרסה ישנה יותר, או שהכפתור לא עוזר

1. פתח את הדשבורד.
2. הקש **F12** ← לשונית **Console**.
3. הדבק את הפקודה הבאה ולחץ Enter — **היא רק בודקת, לא משנה כלום:**

```
var o={};var m=null;try{m=JSON.parse(localStorage.getItem('planner_lock_v1')||'null')}catch(e){};o['נעילה מופעלת']=!!(m&&m.enabled);Object.keys(localStorage).filter(k=>k.indexOf('planner_v1')===0).forEach(k=>{var r=localStorage.getItem(k)||'';o[k]=(r.indexOf('{"enc":1')===0?'מוצפן':'רגיל')+' · '+(r.length/1024).toFixed(1)+' ק״ב'});var v=[];try{v=JSON.parse(localStorage.getItem('planner_versions_v1')||'[]')}catch(e){};o['גרסאות לשחזור']=v.filter(x=>x.data.indexOf('{"enc"')!==0).map(x=>new Date(x.at).toLocaleString('he-IL'));o
```

4. אם ברשימת **"גרסאות לשחזור"** יש תאריכים — הדבק גם את זו, והיא תשחזר:

```
var v=JSON.parse(localStorage.getItem('planner_versions_v1')||'[]').filter(x=>x.data.indexOf('{"enc"')!==0);if(!v.length){alert('אין גרסה לשחזור')}else{var b=new Blob([v[0].data],{type:'application/json'});var a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='מתכנן-חילוץ.json';a.click();localStorage.setItem('planner_v1',v[0].data);localStorage.removeItem('planner_lock_v1');alert('שוחזר מ-'+new Date(v[0].at).toLocaleString('he-IL')+'. הדף ייטען מחדש.');location.reload()}
```

הפקודה מורידה קודם קובץ גיבוי למחשב, ורק אחר כך משחזרת ומבטלת את הנעילה.

> **מה זה אומר על הנעילה:** היא מגנה מפני מבט מזדמן, לא מפני מי שיושב מול המחשב שלך.
> מי שרוצה הגנה חזקה יותר — אחרי הפעלת הנעילה כדאי לרוקן את היסטוריית הגרסאות
> (הגדרות ← היסטוריית גרסאות).

---

## 2. כתוב "נעול לעריכה" אבל לא הגדרתי סיסמה

זה **לא** הנעילה בסיסמה, אלא אחד משני מצבים אחרים:

| מה שכתוב ברצועה | מה זה | איך מבטלים |
|---|---|---|
| 🔒 **מצב תצוגה בלבד** | מתג בהגדרות שנועל את שדות העריכה | **לוחצים על הרצועה עצמה** — היא מבטלת את המצב |
| **מצב שבת פעיל** | נעילה אוטומטית מהדלקת נרות עד ההבדלה | לוחצים על הרצועה, או מכבים בהגדרות ← "תוכן יומי" |

בגרסה 3.5.1 שתי הרצועות ניתנות ללחיצה ומבטלות את עצמן.

---

## 3. עשיתי עדכון ואני עדיין רואה את הגרסה הישנה

ה-Service Worker שומר את האפליקציה במטמון כדי שתעבוד אופליין, ולפעמים הוא ממשיך
להגיש את הגרסה הישנה.

**הפתרון המהיר:** `Ctrl+Shift+R` (רענון קשיח).

**אם זה לא הספיק** — F12 ← Console ← הדבק:

```
(async()=>{var r=await navigator.serviceWorker.getRegistrations();await Promise.all(r.map(x=>x.unregister()));var k=await caches.keys();await Promise.all(k.map(x=>caches.delete(x)));alert('המטמון נוקה. הדף ייטען מחדש.');location.reload(true)})()
```

זה מוחק **רק** את מטמון האפליקציה. הנתונים שלך לא נוגעים.

---

## כלל אצבע אחד שימנע את כל זה

**הגדרות ← נתונים וגיבוי ← "ייצוא גיבוי JSON"**, אחת לשבוע, לתיקייה שאתה זוכר.
קובץ אחד קטן שמחזיר הכול בכל מצב.
