# העלאת V4 ל-GitHub ו-GitHub Pages

מדריך מלא מאפס עד קישור עובד. כל השלבים, כולל מה בדיוק להעלות ומה לא.

---

## מה מעלים ומה לא

**מעלים** — את התוכן של תיקיית `V4` בלבד:

```
index.html          ← האפליקציה
manifest.json       ← נדרש להתקנה כאפליקציה (PWA)
sw.js               ← עבודה בלי אינטרנט
assets/             ← icon-192.png · icon-512.png
```

**לא מעלים:**

* `V4-Desktop` — זו אפליקציית מחשב, לא אתר. אין לה מה לעשות ב-Pages.
* `שינויים.md`, `README.md`, `העלאה-לגיט.md` — אפשר, אבל הם יהיו גלויים לכולם.
* גיבויי JSON — **לעולם לא.** יש בהם את כל הנתונים שלך.

> ⚠️ **המאגר חייב להיות ציבורי** כדי ש-Pages יעבוד בחשבון חינמי. כלומר האפליקציה
> תהיה נגישה לכל מי שיש לו את הקישור. **הנתונים שלך לא עולים** — הם נשמרים
> בדפדפן שלך בלבד. אבל אל תעלה גיבויים לתיקייה.

---

## חלק א׳ — יצירת המאגר (פעם אחת בלבד)

1. היכנס ל-**<https://github.com>** והתחבר.
   אין חשבון? **<https://github.com/signup>**
2. לחץ **+** בפינה השמאלית העליונה ← **New repository**
   קיצור ישיר: **<https://github.com/new>**
3. מלא:

   | שדה | מה למלא |
   |---|---|
   | Repository name | `planner` (אנגלית, בלי רווחים) |
   | Description | דשבורד מתכנן — יומי, שבועי, חודשי ושנתי |
   | Public / Private | **Public** |
   | Add a README file | **אל תסמן** |

4. **Create repository**

---

## חלק ב׳ — העלאת הקבצים

### דרך 1 — דרך האתר (הכי פשוט, מומלץ לפעם הראשונה)

1. במסך המאגר החדש לחץ **uploading an existing file**
2. פתח בסייר הקבצים:

```
D:\mati\claude ptojacts\דשבורד מתכנן V1\עידכוני גירסא\V4
```

3. **גרור לחלון הדפדפן**: `index.html` · `manifest.json` · `sw.js` · תיקיית `assets`
4. בתיבה למטה כתוב `העלאת V4` ולחץ **Commit changes**

> אם תיקיית `assets` לא נקלטת בגרירה: **Add file ← Create new file**, כתוב בשם הקובץ
> `assets/icon-192.png` — הלוכסן יוצר את התיקייה — ואז חזור והעלה אליה את שני ה-PNG.

### דרך 2 — שורת פקודה (מהיר לעדכונים חוזרים)

צריך Git מותקן: **<https://git-scm.com/download/win>**

פתח שורת פקודה בתיקיית `V4` והרץ **פעם אחת**:

```bash
git init
```

```bash
git add index.html manifest.json sw.js assets
```

```bash
git commit -m "העלאת V4"
```

```bash
git branch -M main
```

החלף `USERNAME` בשם המשתמש שלך:

```bash
git remote add origin https://github.com/USERNAME/planner.git
```

```bash
git push -u origin main
```

בפעם הראשונה ייפתח חלון התחברות ל-GitHub. אשר אותו.

---

## חלק ג׳ — הפעלת GitHub Pages

1. במאגר: **Settings** (בסרגל העליון)
2. בתפריט הצדדי: **Pages**
   קיצור ישיר: `https://github.com/USERNAME/planner/settings/pages`
3. תחת **Source** בחר **Deploy from a branch**
4. **Branch:** `main` · **Folder:** `/ (root)` ← **Save**
5. המתן 1–2 דקות. רענן את הדף — למעלה יופיע הקישור:

```
https://USERNAME.github.io/planner/
```

---

## חלק ד׳ — התקנה כאפליקציה

פתח את הקישור **בטלפון או בטאבלט**:

* **אנדרואיד / כרום** — תפריט ⋮ ← "התקן אפליקציה" / "הוספה למסך הבית"
* **אייפון / ספארי** — שיתוף ⬆ ← "הוספה למסך הבית"

מקבלים אייקון, מסך מלא בלי סרגל הדפדפן, ועבודה גם בלי אינטרנט.

---

## עדכון לגרסה הבאה

### דרך האתר

**Add file ← Upload files** ← גרור את הקבצים המעודכנים ← **Commit changes**.
העלאה של קובץ עם אותו שם דורסת את הקודם.

### דרך שורת פקודה

```bash
git add -A
```

```bash
git commit -m "עדכון V4.1"
```

```bash
git push
```

> **חשוב אחרי כל עדכון:** ה-Service Worker שומר את הגרסה הישנה במטמון.
> אחרי הפריסה — `Ctrl+Shift+R` בדפדפן, ובטלפון: סגור את האפליקציה לגמרי ופתח מחדש.
> אם עדיין רואים ישן: **הגדרות ← איפוס מטמון האפליקציה**.
>
> **בכל גרסה חדשה יש לעדכן את `VERSION` בקובץ `sw.js`** — אחרת המטמון לא מתחלף.
> ב-V4 הוא `v4.0.0`.

---

## תקלות נפוצות

| מה קורה | למה | מה עושים |
|---|---|---|
| 404 בקישור | Pages עדיין נבנה, או ה-Branch לא נשמר | המתן 2 דקות, ובדוק ב-Settings ← Pages שהענף הוא `main` ו-`/ (root)` |
| הדף עולה בלי עיצוב | `index.html` הועלה לתוך תת-תיקייה | הקובץ חייב לשבת בשורש המאגר, לא בתוך `V4/` |
| רואים גרסה ישנה | Service Worker | `Ctrl+Shift+R`, ובדוק שעדכנת את `VERSION` ב-`sw.js` |
| אין אפשרות "התקן אפליקציה" | `manifest.json` או `assets` לא עלו | ודא ששניהם במאגר, וש-`sw.js` נטען בלי שגיאה (F12 ← Console) |
| "Permission denied" ב-push | לא מחובר | `git config --global user.name "השם"` ו-`git config --global user.email "המייל"`, ואז push מחדש |

---

## קישורים

| מה | לאן |
|---|---|
| יצירת מאגר חדש | <https://github.com/new> |
| התקנת Git ל-Windows | <https://git-scm.com/download/win> |
| GitHub Desktop (ממשק גרפי, בלי שורת פקודה) | <https://desktop.github.com> |
| תיעוד GitHub Pages | <https://docs.github.com/pages> |
| הגדרות Pages של המאגר שלך | `https://github.com/USERNAME/planner/settings/pages` |

---

## אם אתה מעדיף בלי שורת פקודה בכלל

**GitHub Desktop** (<https://desktop.github.com>) נותן הכול בלחיצות:

1. התקן והתחבר לחשבון
2. **File ← Add local repository** ← בחר את תיקיית `V4`
3. הוא יציע ליצור מאגר — אשר
4. בצד שמאל תראה את הקבצים שהשתנו; כתוב תיאור ולחץ **Commit to main**
5. למעלה: **Publish repository** — ודא ש-"Keep this code private" **לא מסומן**
6. בעדכון הבא: Commit ← **Push origin**
